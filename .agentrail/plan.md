Build the RV32I ISA crate first because assembler, emulator, target, and codegen depend on a shared instruction contract. Work proceeds in small TDD steps: scaffold the crate, define registers and ABI aliases, define the instruction model and format helpers, implement decode, implement encode, implement disassembly, add illegal/reserved decode coverage, and add encode/decode round-trip coverage.