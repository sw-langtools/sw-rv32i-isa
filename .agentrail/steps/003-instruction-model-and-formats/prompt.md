Define the RV32I Instruction enum and supporting operand enums/helpers for U, J, B, I, S, R, load/store widths, branch conditions, immediate operations, register operations, FENCE, ECALL, and EBREAK. Model semantic operands rather than raw bit fields, matching the documented emulator and assembler contract. Add tests for helper behavior and immediate sign/bit placement rules where useful.