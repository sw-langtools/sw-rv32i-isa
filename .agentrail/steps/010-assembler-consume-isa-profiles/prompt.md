Update sw-rv32i-asm to consume the shared IsaProfile API from sw-rv32i-isa. Add an assembly entry point that accepts a profile, default the existing assemble API to rv32i, validate emitted instructions against the selected profile, add tests showing rv32e rejects x16-x31 operands while rv32i accepts existing MVP hello-world source, and keep profile parsing/CLI work for a later step unless already present.